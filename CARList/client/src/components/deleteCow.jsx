// import React from "react";
// <div>
// <h4>Add more cows!</h4>
// Enter a cows name: <input defaultValue={name} onChange={m} />
// <button onClick={ ()=> props.deleting(name)}> delete cow </button>

// </div>
